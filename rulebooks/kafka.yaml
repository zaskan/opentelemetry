---
- name: Rulebook for Netops demo
  hosts: localhost

  sources:
     - name: Monitor kafka otel-messages topic
       ansible.eda.kafka:
         host: 192.168.122.247
         port: 9092
         check_hostname: false
         verify_mode: CERT_NONE
         topic: otel-messages

  rules:

    - name: Trigger BGP validation
      condition: event.body != ""
      action:
        debug:
