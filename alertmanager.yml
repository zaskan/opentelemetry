global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'otel'

receivers:
  - name: 'otel'
    webhook_configs:
      - url: 'http://otel-collector:4318/v1/traces'
        send_resolved: true

